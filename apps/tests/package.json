{
  "name": "tests",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.18.1"
  },
  "scripts": {
    "dev": "bun --env-file=../../.env run vitest --run tests/tests.ts",
    "build": "bun build tests.ts --outdir dist --target=bun",
    "test": "echo 'Add tests script here'",
    "lint": "echo 'Add lint script here'"
  },
  "dependencies": {
    "@prisma-db": "workspace:*",
    "@redis-client": "workspace:*",
    "@shared-types": "workspace:*",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.18.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
