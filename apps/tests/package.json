{
  "name": "tests",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },  "scripts": {
    "dev": "vitest --run tests/tests.ts",
    "build": "bun build tests.ts --outdir dist --target=bun",
    "test": "echo 'Add tests script here'",
    "lint": "echo 'Add lint script here'"
  },
  "dependencies": {
    "@prisma-db": "workspace:*",
    "@redis-client": "workspace:*",
    "@shared-types": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
